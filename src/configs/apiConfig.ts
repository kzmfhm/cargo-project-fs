type APIConfig = {
  HOST: string
}

const apiConfig: APIConfig = {
  // ** API Configs
  // HOST: 'http://localhost:8000' /* Django Site URL */,
  HOST: 'https://app.kingsofcargo.com' /* Django Site URL */,
}

export default apiConfig
