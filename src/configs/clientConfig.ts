type ClientConfig = {
  CLIENT: string
}

const clientConfig: ClientConfig = {
  // ** Client Configs
  CLIENT: 'https://ship.maalbardaar.com' /* Next.js React Site URL */,
  // CLIENT: 'http://localhost:3000' /* Next.js React Site URL */,
}

export default clientConfig
